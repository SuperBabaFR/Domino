from django.urls import path, re_path


websocket_urlpatterns = [
    # path('ws/chat/', ChatConsumer.as_asgi()),  # Chemin WebSocket
    # path('ws/session/<str:session_name>/', SessionConsumer.as_asgi()),
]
